source = ~/.config/hypr/monitor.conf
source = ~/.config/hypr/looknfeel.conf
source = ~/.config/hypr/binds.conf

#AUTOSTART
exec-once = hyprpaper
exec-once = qs -c noctalia-shell
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = flatpak run com.core447.StreamController -b
exec-once = ~/.config/hyprland/scripts/start_easyeffects.sh

env = XCURSOR_SIZE,32
env = HYPRCURSOR_SIZE,32

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

input {
    kb_layout = us, ru
    kb_variant =
    kb_model =
    kb_options = grp:alt_shift_toggle
    kb_rules =
    numlock_by_default = true
    follow_mouse = 1
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    touchpad {
        natural_scroll = false
    }
}

device {
    name = -------ajazz-2.4g-8k
    sensitivity = -0.3
    accel_profile = flat
}

device {
    name = wacom-one-by-wacom-m-pen
    output = DP-1
}

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# Window rules per app

windowrule = match:class ^(com.core447.StreamController)$, float 1, center 1, size 900 650
windowrule = match:class ^(org.gnome.Calendar)$, float 1, center 1, size 600 800
windowrule = match:class ^(org.pulseaudio.pavucontrol)$, float 1, center 1, size 900 650

# Waybar in .config/waybar

source = /home/theyopi/.config/hypr/noctalia/noctalia-colors.conf

